# 2026-08-29

- Fixed `.github/workflows/objective-c-xcode.yml` (Xcode build & analyze workflow).
- Problems in the old file: (1) used `targets[0]` as scheme instead of the shared scheme `FPPDFFrameworkDemoiOS`; (2) no `-destination`, so xcodebuild targeted generic iOS device and failed on code signing (project hardcodes DEVELOPMENT_TEAM=3H8C8HR656, "iPhone Developer"); (3) `xcpretty` is not installed on GitHub macOS runners.
- Fix: explicit project/scheme env vars, `-destination "generic/platform=iOS Simulator"`, `CODE_SIGNING_ALLOWED=NO`, `set -o pipefail` + `tee xcodebuild.log` + `xcbeautify` with fallback, log artifact upload on failure, workflow_dispatch + concurrency group.
- Verified locally: `xcodebuild clean build analyze -project FPPDFFrameworkDemoiOS.xcodeproj -scheme FPPDFFrameworkDemoiOS -destination "generic/platform=iOS Simulator" -configuration Debug CODE_SIGNING_ALLOWED=NO` exits 0 (Xcode 26.5 SDK, iOS deployment target 12.0). One static analyzer warning: retained `navigationController` ivar not released before `[super dealloc]` in FPPDFFrameworkDemoiOS/FPFAppDelegate.m:47.
- Project facts: single xcodeproj at repo root (FPPDFFrameworkDemoiOS.xcodeproj), shared scheme FPPDFFrameworkDemoiOS, FPPDFFramework.xcframework (ios-arm64 + ios-arm64_x86_64-simulator slices) inside FPPDFFrameworkDemoiOS/ folder, app bundle id com.flyingbee.FPPDFConverterDemo.
